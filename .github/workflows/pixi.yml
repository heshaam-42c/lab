name: Pixi Token #1

on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - id: step1
      run: | 
        response=$(curl -k -d "user=user@acme.com&pass=hellopixi" -X POST https://photo-demo.westeurope.cloudapp.azure.com/hesh/api/login | jq -r '.token')
        echo "response from curl= $response"
        
        docker pull 42crunch/scand-agent:v2.0.0
        docker run -e SCAN_TOKEN=scan_59f65ec3-983b-4328-881a-0d4812b74ed2 -e SECURITY_TOKEN=$response 42crunch/scand-agent:latest
